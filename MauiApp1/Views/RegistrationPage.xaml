<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MauiApp1.ViewModels"
             x:Class="MauiApp1.Views.RegistrationPage"
             Title="ลงทะเบียนรายวิชา">

    <StackLayout Padding="10">
        <Label Text="รายวิชาที่เปิดให้ลงทะเบียน" FontSize="20" FontAttributes="Bold" />
                            <Grid Grid.Row="0"
                           ColumnDefinitions="Auto,*"
                           RowDefinitions="Auto,*">

                            <!-- Back Button -->
                            <Button Grid.Row="0"
                                    Text="ย้อนกลับ"
                                    Command="{Binding GoBackCommand}"
                                    BackgroundColor="{AppThemeBinding Light=#0071e3, Dark=#0A84FF}"
                                    TextColor="White"
                                    FontSize="16"
                                    CornerRadius="20"
                                    HeightRequest="44"
                                    WidthRequest="100"
                                    HorizontalOptions="Start"
                                    VerticalOptions="Start"/>
                     </Grid>
        <CollectionView ItemsSource="{Binding AvailableCourses}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal" Padding="5">
                        <Label Text="{Binding CourseId}" FontSize="16" />
                        <Label Text="{Binding Name}" FontSize="16" Margin="10,0,0,0" />
                        <Button Text="ลงทะเบียน"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:RegistrationPageViewModel}}, Path=RegisterCommand}"
                                CommandParameter="{Binding CourseId}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="รีเฟรช" Command="{Binding RefreshCommand}" Margin="0,10,0,0" />
    </StackLayout>
</ContentPage>